services:
  mysql:
    image: mysql:8.4
    container_name: alfa-mysql
    ports:
      - "3306:3306"
    env_file:
      - backend/.env
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
