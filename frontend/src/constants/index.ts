export const BASE_CACHE_TIME_MS = 60_000; //1 минута

export const PRICE_SUFFIX = '₽';

export const PRICE_PER_M2_SUFFIX = `${PRICE_SUFFIX}/м²`;

export const AVAILABLE_PAGES = [{ label: 'Недвижимость', href: '/properties/' }] as const;
